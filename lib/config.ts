// One place to brand the app. Everything else is in-app now.
export const config = {
  brandName: "Tripcraft", // ← rename to your brand
  contactEmail: "hello@tripcraft.app", // ← used on the privacy + terms pages
  price: "IDR 750.000",
  moneyBackGuarantee: true,
};
